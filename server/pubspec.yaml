name: boba_receipt_server
description: Skeleton backend xác thực biên nhận IAP cho Boba Empire.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  # Dùng cho xác thực Google Play thật (service account → androidpublisher API).
  googleapis_auth: ^1.6.0
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
